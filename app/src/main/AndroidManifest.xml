<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tal.yudale">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Search"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Search_Results"
            android:label="@string/title_activity_search__results"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".payment_send"
            android:label="@string/title_activity_payment_send"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".payment_rec"
            android:label="@string/title_activity_payment_rec"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".main_screen"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>